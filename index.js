document.write("Hello, world!");

const subRef = document.querySelector
("#subtitle");

subRef.innerHTML = "Document Object Model (DOM) - web APIs - MDN Web DOCS";

const paragraph= document.querySelector
("#description");
paragraph.innerHTML = " hi,My name is Rukhsana. i want to become development";

const ageRef= document.querySelector
("#age")
console.log(ageRef.innerHTML);

const age=Number(ageRef.innerHTML)
console.log(typeof age)

